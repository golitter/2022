一个典型的函数定义包括以下部分：修饰符、返回类型、函数名字、由0个或多个形参组成的列表以及函数体。
    修饰符： private static 
        e.g. func 
            private static void Name(...) {... return;}
            class内全局变量
                private static int sInt = Value;
            未学class，记住 private static... 修饰符
参数传递:
    值传递: 八大基本数据类型和String类型等采用值传递。
    引用传递: 除String以外的数据类型的对象，例如数组、StringBuilder等采用引用传递。
        将实参的引用（地址）传给形参，通过引用找到变量的真正地址，然后对地址中的值修改。所以此时对形参的修改会影响实参的初始值。
        或者基本类型的对象类型
返回类型:
    return语句终止当前正在执行的函数并将控制权返回到调用该函数的地方。return语句有两种形式：
    无返回值: void类型


与 C 类似可能
