/**
 * @brief 
 * 常用的反转函数，
 * 普通数组， reverse(arr+0,arr+n);
 * 有迭代器的数据类型 reverse(vect.begin(),vect.end());
 * @return int 
 */
int main()
{

    int ar[4] = {1,34,23,3};
    reverse(ar+0, ar+4);
    for(int i = 0; i < 4; i++) cout<<ar[i];
    return 0;
}
