[Dashboard - Codeforces Round 906 (Div. 2) - Codeforces](https://codeforces.com/contest/1890)

A题，签到。

```cpp
void solve() {
    int n; cin>>n;
    vector<int> a(n);
    map<int,int> mp;
    for(auto &t: a) cin>>t, mp[t]++;
    if(mp.size() > 2) {
        puts("No");
    } else {
        if(mp.size() == 1) puts("Yes");
        else {
            vector<int> v(2);
            int a1 = (*mp.begin()).vs;
            if(a1 == n / 2 || a1 == n - n / 2) puts("Yes");
            else puts("No");
        }
    }
}
```

B题，模拟。

```cpp
void solve() {
    int n,m; cin>>n>>m;
    string s,t; cin>>s>>t;
    bool okt = true;
    for(int i = 1; i < m; ++i) okt &= t[i] != t[i-1];
    bool ok = true;
    for(int i = 1; i < n; ++i) {
        if(s[i] != s[i-1]) continue;
        // i-1 insert
        if(okt && s[i-1] != t[0]) {
            if(s[i] != t.back()) {
                s[i-1] = t.back();
                // --i;
            } else ok = false;
        } else {
            ok = false;
        }
    }
    puts(ok ? "Yes" : "No");
}
```

C题，贪心。

真正学会了string.insert()。（

```cpp
void solve() {
    int n; cin>>n;
    string s; cin>>s;
    s = " " + s;
    int p = 0;
    vector<int> pos;
    bool ok = true;
    while(p <= 300) {
        int l = 1, k = s.size() - 1;
        int khf = (k + 1) / 2;
        int ep = 0;
        for(int i = 1; i <= khf; ++i) {
            if(s[i] == s[k - i + 1]) {
                ep = i;
                break;
            }
        }
        if(!ep) {
            break;
        } else {
            int ed = k - ep + 1;
            int st = ep;
            if(s[st] == '0') {
                pos.push_back(ed);
                s.insert(ed + 1, "01");
            } else {
                pos.push_back(st - 1);
                s.insert(st, "01");
            }
        }
        p++;
        if(p > 300) {
            ok = false;
            break;
        }
    }
    if(ok) {
        cout<<pos.size()<<endl;
        for(auto &t: pos) cout<<t<<" "; puts("");
    } else {
        puts("-1");
    }
}
```

D题，距离AC，只差一个trick了，那就是弄完1后没有贪心。（

```cpp
void solve() {
    int n,c; cin>>n>>c;
    vector<int> a(n + 1);
    int sum = 0;
    for(int i = 1; i <= n; ++i) cin>>a[i], sum += a[i];
    int now = a[1];
    vector<int> vis(n + 1);
    vis[1] = 1;
    bool fd1 = false;
    for(int i = 2; i <= n; ++i) {
        // 1 和 i
        int tmp = now + a[i];
        if(tmp >= 1 * i * c) {
            now += a[i];
            vis[i] = 1;
            fd1 = true;
        }
    }
    vector<array<int,2>> b(n + 1);
    for(int i = 1; i <= n; ++i) b[i] = {a[i], i};
    sort(b.begin() + 1, b.end(), [&](auto pre, auto suf) {
        return pre[0] - pre[1] * c > suf[0] - suf[1] * c;
    });
    if(fd1) {
        for(int i = 1; i <= n; ++i) {
            int idx = b[i][1];
            int val = b[i][0];
            if(vis[idx]) continue;
            int tmp = now + val;
            if(tmp >= 1 * idx * c) {
                now += val;
                vis[idx] = 1;
            }
        }
    }
    bool ok = true;
    for(int i = 1; i <= n; ++i) ok &= vis[i];
    puts(ok ? "Yes" : "No");
}
```

# 总结

还是原来的毛病，没有证明就写，导致wawawa。

下次一定要注意这个情况。

加油！！！