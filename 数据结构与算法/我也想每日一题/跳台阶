/**
* 台阶问题，可以像寒假学dp的方法，
* 先找到前五个(大致）的规律，之后类推n
* 然后也可以倒退，
* 有时候倒推更简单点：
* 同样的，这个题有好几个方法
* 一个是递归，一个是递推，
* 另一个是优化递推;
*/
class Solution {
public:
    int jumpFloor(int number) {
        int a =1, b = 1, c = 1;
        for(int i = 2; i <= number; ++i) {
            c = a + b;
            a = b;
            b = c;
        }
        return c;
    }
};
