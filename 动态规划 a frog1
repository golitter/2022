#include<bits/stdc++.h>
using namespace std;
const int M=5e5+3;
int l[M];
int dp[M];
int main()
{
  int n;
  cin>>n;
  for(int i = 1; i <= n; i++) cin>>l[i];
  dp[2] = abs(l[2]-l[1]);
  for(int i = 3; i <= n; i++) dp[i]=min(dp[i-1]+abs(l[i]-l[i-1]),dp[i-2]+abs(l[i]-l[i-2]));
  cout<<dp[n];
}
