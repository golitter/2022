[Dashboard - Codeforces Round 907 (Div. 2) - Codeforces](https://codeforces.com/contest/1891)

A题，签到。（不过写成通用的了，数据范围很小，一两分钟都能敲完。

```cpp
void solve() {
    int n; cin>>n;
    vector<int> a(n + 1);
    for(int i = 1; i <= n; ++i) cin>>a[i];
    bool ok = true;
    for(int i = 0; i <= n; ++i) {
        int k = pow(2, i);
        int j = k / 2;
        for(int z = j + 1; z < min(k,n); ++z) {
            if(a[z] > a[z + 1]) {
                ok = false;
                break;
            }
        }
    }
    puts(ok ? "YES" : "NO");
}
```

B题，我是暴力遍历，但是不知道是不是数据弱了。（明天看看std

```cpp
void solve() {
    int n,m; cin>>n>>m;
    vector<int> a(n),b(m), vis(n);
    for(auto &t: a) cin>>t;
    map<int,int> mp;
    for(auto &t: b) cin>>t, mp[t]++;
    int cnt = 0;
    for(int i = 0; i < m; ++i) {
        int x = b[i];
        int w = (1 << x);
        int ad = (1 << ( x - 1));
        mp[b[i]]--;
        for(int j = 0; j < n; ++j) {
            if(vis[j]) continue;
            if(a[j] % w == 0) {
                a[j] += ad;
            }
            bool ok = false;
            for(int z = 1; z <= 30; ++z) {
                int w = (1 << z);
                if(a[j] % w == 0 && mp[z]) {
                    ok = true;
                    break;
                }
            }
            if(!ok) vis[j] = 1,cnt++;
        }
        if(cnt == n) break;
    }
    for(auto &t: a) cout<<t<<" "; puts("");
}
```

C题，贪心即可，不知道为什么前几个不对（明天看看

```cpp
void solve() {
    int n; cin>>n;
    vector<int> a(n);
    int sum = 0;
    for(auto &t: a) cin>>t, sum += t;
    sort(all(a));
    int l = 0, r = n - 1;
    int x = 0, cnt = 0;
    for(int i = n - 1; i >= 0; --i) {
        if(sum >= a[i] * 2) {
            sum -= a[i] * 2;
            cnt += a[i] + 1;
        } else {
            if(sum) {
                int t = (sum + 1) / 2;
                cnt += t;
                sum -= t;
                if(sum) cnt += 1;
            }
            break;
        }
    }
    cout<<cnt<<endl;
}
```

（其他明天补

