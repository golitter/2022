/**
 * @file Untitled-3
 * @author your name (you@domain.com)
 * @brief 
 * @version 0.1
 * @date 2022-03-27
 * 
 * @copyright Copyright (c) 2022
 * memmove(str, str1,n); 为将str1的n个元素复制到str后，如果目标区域和源区域有重叠的话，
 * memmove能够保证源串在被覆盖之前将重叠区域的字节拷贝到目标区域中。
 * 但复制后src内容会被更改。但是当目标区域与源区域没有重叠则和memcpy函数功能相同。
 * 
 */
#include <iostream>
using namespace std;
void NumberSystem(char *in, int insys, char *out, int outsys) {
    int v = 0, n;
    for(int char *p = in; *p; p++) 
        v = v*insys + (*p - ((*p >= 'a') ? ('a' - 10 ): '0')); // v 最后表示为 在十进制下的值；
    for(n = 31; v&&n; v = v/outsys) 
        out[n--] = (((v%outsys) < 10) ? '0' : ('a'  - 10)) + (v%outsys);// 
    memmove(out, out + n + 1, 32 - n - 1);
    out[32-n-1] = 0;
}
int main()
{

}
