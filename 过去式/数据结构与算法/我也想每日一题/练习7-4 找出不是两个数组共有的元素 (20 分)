// I donot find  basics of PTA alse difficult for me;
// Two arrarys :find they difference numbers 
// and print they;
#include <stdio.h>
int a[21];
int b[21];
char aa[21];
char bb[21];
int last[44];
int main()
{
  int n, m;
  int frag = 1;
  scanf("%d", &n);
  for(int i = 0; i < n; i++) {
    scanf("%d",&a[i]);
  }
  scanf("%d",&m);
  for(int i = 0; i < m; i++) {
    scanf("%d",&b[i]);
  }
  for(int i = 0; i < n; i++) {
    for(int j = 0; j < m; j++) {
      if(a[i] == b[j]) {
        aa[i] = '/'; bb[j] = '/';
      }
    }
  }
  int cont = 0;
  for(int i = 0; i < n; i++) {
    if(aa[i] != '/') {
      for(int z = 0; z < cont; z++) {
        if(last[z] == a[i]) {
          frag = 0;
        }
      }
      if(frag == 1)
      last[cont++] = a[i];
      frag = 1;
    }
  }
  frag = 1;
  for(int j = 0; j < m; j++) {
    if(bb[j] != '/') {
      for(int z = 0; z < cont; z++) {
        if(last[z] == b[j]) {
          frag = 0;
        }
      }
      if(frag == 1)
      last[cont++] = b[j];
      frag = 1;
    }
  }
  for(int i = 0; i < cont - 1; i++) {
    printf("%d ",last[i]);
  }
  printf("%d",last[cont-1]);
}
/*
  Originally, I felt the question equal programme of winter vacation training
  And I also do not find the difference numbers has two groups.
  To be honest, I felt my answer is not difficutly, because I swap a method;
  If I dont swap , I will very difficulty to AC;
  So all roads lead to Rome;
*/
